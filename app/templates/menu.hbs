<ul>
  <li>{{#link-to 'menu.create'}}New Item{{/link-to}}</li>
  {{#each model as |item|}}
    {{#unless item.isNew}}
      <li>
        {{#link-to 'menu-item.show' item}}
          {{item.title}}
        {{/link-to}}
      </li>
      {{/unless}}
  {{/each}}
</ul>


{{!-- {{outlet}} --}}

import { toDollars } from '../../../helpers/to-dollars';
import { module, test } from 'qunit';

module('Unit | Helper | to dollars');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toDollars(42);
  assert.ok(result);
});
